# Backend

Go REST API for Okresownik.

The API serves JSON over HTTP. Authentication endpoints return a JWT, and protected endpoints expect `Authorization: Bearer <token>`.

Interactive API documentation is available through Swagger UI at <https://api.okresownik.pl/swagger>.
